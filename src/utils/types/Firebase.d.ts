declare type DefaultParamsRemoteConfig = {
  theme_dark: string;
  theme_light: string;
};

declare type ClientProvider = {
  clientId: string;
  logo?: string;
  logoMenu?: string;
  lightTheme?: string;
  darkTheme?: string;
};
